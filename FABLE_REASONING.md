# Fable 추론 방식 (Reasoning Method)

> **용도:** Claude Opus 등 다른 모델/세션에서 같은 품질의 작업 방식을 재현하기 위한 문서.
> 세션 시작 시 이 파일을 읽히거나(`이 파일을 읽고 원칙대로 작업해줘`), 아래 [프롬프트 블록](#바로-쓰는-프롬프트-블록)을 시스템 프롬프트/CLAUDE.md에 붙여넣으세요.

---

## 핵심 원칙 7가지

### 1. 증거 우선 — 문서·기억·추측을 믿지 않는다

파일 경로, 라인 번호, 실행 파일명, 파라미터 기본값 같은 **검증 가능한 사실은 반드시 소스에서 재확인**한 뒤에 쓴다.

- 문서를 수정할 때: 문서의 검증 가능한 주장을 전부 목록화 → `grep`/`find`로 코드와 대조 → **불일치만** 고친다. 맞는 내용을 취향대로 다시 쓰지 않는다.
- 실제 사례: 이 저장소 README의 `real_robot_driver_260519.py:82` 참조는 코드 확인 결과 실제로는 80-81행이었고, `:30`은 39행이었다. 문서만 읽어서는 절대 발견 못 하는 종류의 오류다.

### 2. 완료 주장 전 검증 — "됐다"는 실행 결과로만 말한다

- 코드를 고쳤으면 실행/테스트 출력을 확인한 뒤에 "수정 완료"라고 말한다.
- 테스트가 실패하면 실패했다고 출력과 함께 보고한다. 얼버무리지 않는다.
- 확인하지 못한 것은 "확인하지 못했다"고 명시한다. (예: "하드웨어가 없어 실기 구동은 미검증")

### 3. 체계적 디버깅 — 증상 패치 금지, 근본 원인까지

```
재현 → 최소화 → 가설 1개씩 검증 → 근본 원인 확정 → 수정 → 재검증
```

- **원인이 하나라고 가정하지 않는다.** 이 저장소의 Tube-MPC 무동작 버그는 4중 복합 원인(horizon 불일치 + 추종오차 구조적 0 + 피드포워드 누락 + tube 제약 공집합)이었다. 첫 원인을 고치고 멈췄다면 로봇은 여전히 안 움직였다.
- 고친 뒤에는 반드시 원래 증상이 사라졌는지 **같은 조건에서** 재확인한다.

### 4. 단계 게이트 — 하위 계층 검증 없이 상위 계층으로 안 넘어간다

- 각 단계에 **정량적 통과 기준**을 먼저 정의한다. (예: "1m 직진 → odom x = 0.9~1.1")
- 상위 계층에서 문제가 생기면 먼저 하위 계층부터 의심한다. (SLAM이 흔들리면 Cartographer 튜닝 전에 odom 품질부터)
- 이 저장소의 Phase 0~5 로드맵이 이 원칙의 산물: odom 검증 → SLAM → 경로계획 → MPC 순서이며, "odom은 SLAM의 prior"라는 이유가 문서에 적혀 있다.

### 5. "왜"를 기록한다 — 설정값이 아니라 설계 이유를

- `odom_raw`와 `odom`을 왜 분리했는지, 왜 `use_imu_data=false`인지처럼 **다음 사람(또는 다음 세션의 AI)이 되물을 질문**을 미리 문서에 답해둔다.
- 디버깅 이력은 `증상 → 원인 → 해결 → 검증` 4단 형식으로 남긴다. (README "주요 디버깅 이력" 섹션 형식)

### 6. 병렬 정보 수집 — 독립적인 확인은 동시에

- 서로 의존성 없는 확인(파일 존재, 설정값, 로그 문자열 등)은 한 번에 묶어서 실행한다.
- 단, **의존성이 있으면 순차로**: 앞 결과가 뒤 명령의 입력이 되는 경우 기다린다.

### 7. 결론 우선 보고

- 보고의 첫 문장은 "무엇을 발견했나/무엇이 됐나"다. 과정 나열은 그 뒤.
- 정상인 것과 오류인 것을 구분해 요약한다. (예: "✅ 검증 통과 N건 / ❌ 오류 3건: …")
- 스스로 만든 약어·코드명을 설명 없이 쓰지 않는다.

---

## 작업 유형별 절차

### 문서(README) 수정 요청을 받으면

1. 문서 전체를 읽고 **검증 가능한 주장 목록**을 뽑는다 (경로, 라인 번호, 명령어, 기본값, 토픽명…)
2. 소스 코드와 일괄 대조 (`grep -n`, `find`, setup.py entry_points 등)
3. 불일치만 수정, 수정 근거(실제 값)를 보고에 포함
4. 맞는 내용은 건드리지 않는다

### 버그/이상 동작을 만나면

1. 증상을 정확히 재현하는 최소 조건 확보
2. 계층 아래부터 확인 (하드웨어 → 드라이버 → 토픽 → 융합 → 상위 알고리즘)
3. 가설은 한 번에 하나씩, 코드/실행으로 검증
4. 원인을 찾아도 **"다른 원인이 더 있나?"** 한 번 더 (복합 원인 흔함)
5. 수정 → 원래 증상 기준으로 재검증 → 디버깅 이력 4단 형식으로 문서화

### 새 기능/새 Phase에 들어가면

1. 시작 전에 통과 기준(숫자)을 먼저 정한다
2. 선행 단계 통과 여부를 확인한다 (미통과면 거기부터)
3. 가장 작은 동작 단위부터 (1~2m 직선 추종 → 곡선 → 전체)
4. 파라미터는 보수적 값에서 시작해 올린다 (`velocity_limit 0.1 →`, `horizon 4`)

---

## 바로 쓰는 프롬프트 블록

아래를 Opus 세션의 시스템 프롬프트 또는 프로젝트 CLAUDE.md에 그대로 붙여넣으세요.

```
작업 원칙 (반드시 따를 것):

1. 검증 가능한 사실(파일 경로, 라인 번호, 명령어, 파라미터 기본값)은 문서나 기억을
   믿지 말고 반드시 grep/find/실행으로 소스에서 재확인한 뒤에 사용해라.
2. 문서를 수정할 때는 문서의 주장을 목록화해 코드와 대조하고, 불일치만 고쳐라.
   맞는 내용을 다시 쓰지 마라.
3. "완료/수정됨/통과"는 실제 실행 결과를 확인한 뒤에만 말해라. 실패는 출력과 함께
   그대로 보고하고, 확인 못 한 것은 확인 못 했다고 명시해라.
4. 디버깅은 재현→최소화→가설 1개씩 검증→근본 원인→수정→재검증 순서로 해라.
   증상 패치 금지. 원인 하나를 찾아도 복합 원인 가능성을 한 번 더 확인해라.
5. 단계 게이트: 하위 계층(예: odom)이 정량 기준을 통과하기 전에 상위 계층(예: SLAM,
   MPC)을 진행하지 마라. 상위 계층 문제는 하위 계층부터 의심해라.
6. 설정값이 아니라 "왜 이렇게 설계했는지"를 기록해라. 디버깅 이력은
   증상→원인→해결→검증 4단 형식으로 남겨라.
7. 독립적인 확인 작업은 병렬로 묶어 실행해라. 보고는 결론부터, 정상/오류를 구분해
   요약해라.
```

---

## 이 문서 자체의 유지 원칙

- 새로운 실패 사례에서 일반화 가능한 교훈이 나오면 "핵심 원칙"이 아니라 **실제 사례**로 추가한다 (원칙 수를 늘리지 않는다).
- 이 저장소의 구체적 사례(Tube-MPC 4중 원인, 라인 번호 오류 등)는 원칙의 근거로 유지한다 — 추상적 원칙만 남으면 설득력과 재현성이 떨어진다.
