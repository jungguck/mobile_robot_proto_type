# 🤖 Relay Robot Hardware

이 페이지는 프로젝트에 사용된 주요 하드웨어 구성 요소와 사양을 기록합니다.

## DDSM400 모터
- **타입**: Direct Drive Smart Motor (차륜형 로봇용).
- **특징**: 고토크, 정밀 인코더 내장.
- **통신**: Waveshare DDSM 드라이버 HAT 을 통한 JSON over Serial (`/dev/motor`, 115200).

### 통신 규약 (2026-08-22 실측 확정)
| 항목 | 값 |
|------|-----|
| 보드 타입 설정 | `{"T":11002,"type":210}` — **전원 사이클당 1회 필수** |
| 속도 제어 | `{"T":10010,"id":N,"cmd":X,"act":10}` |
| 모드 설정 | `{"T":10012,"id":N,"mode":2}` (2 = speed loop) |
| `cmd` / `spd` 단위 | **0.1 RPM** (cmd 100 = 10 RPM) |
| 피드백 | `{"T":20010,"id":..,"typ":400,"spd":..,"crt":..,"tep":..,"err":0}` |
| 피드백 시점 | **명령을 받았을 때만** 회신 (주기적 전송 없음) |
| heartbeat | 기본 `-1` = **비활성**. 정지 명령 없으면 무한 회전 |

> ⚠️ **`type` 은 반드시 `210`.** 모터는 스스로를 `typ:400` 이라고 보고하지만,
> 펌웨어(`ddsm_example.ino: set_ddsm_type`)는 `115` / `210` 만 인정하고
> 그 외 값은 **조용히 무시**한다. 이걸 빼먹으면 보드가 구동 명령에 응답조차 하지 않는다.
> 자세한 내용: [[Debugging_Experience#7]]

### 모터 배치 / 부호 규약 (2026-08-22 실측 확정)
| 항목 | 값 |
|------|-----|
| id = 1 | **오른쪽** 바퀴 |
| id = 2 | **왼쪽** 바퀴 |
| 전진 부호 | R = **음수**, L = **양수** |

> 좌우 허브모터가 거울 대칭으로 장착돼 있다.
> 이 부호는 `MotorDriver` 의 `DIR_R = -1` / `DIR_L = +1` 이 전부 흡수하므로,
> **드라이버 바깥에서는 부호를 건드리지 마라.** ("+ = 로봇 전진") [[Debugging_Experience#9]]

### 기구학 설정값
| 항목 | 값 | 상태 |
|------|-----|------|
| 바퀴 반지름 | 0.0325 m (지름 65mm) | ⚠️ **캘리브레이션 미완** |
| 양 바퀴 중심거리 | 0.22 m | 실측 |
| spd → m/s 변환 계수 | `/600.0` | 확정 |

> ⚠️ 반지름은 아직 검증되지 않았다. odom 52.5cm 주행 시 실제 거리가 눈대중 약 60cm 였는데,
> 이 값이 맞다면 반지름이 0.0371(지름 7.4cm)이 되어 실측 6.5cm 와 14% 어긋난다.
> **바퀴 지름 재측정 + 줄자 실측이 필요하다.** (`motor_test0822.py` 사용)

## RPLidar
- **모델**: RPLidar A1/A2 계열.
- **용도**: 2D 주변 환경 스캔 및 SLAM용 데이터 제공.
- **Topic**: `/scan`
- **Frame ID**: `lidar_v1_1`

## EB-IMU
- **모델**: E2BOX EBIMU9DOFV5. 포트 `/dev/ttyimu` (CP2102, 115200).
- **역할**: EKF 의 방향(yaw) 소스. 바퀴 오도메트리의 회전 오차를 보정.
- **Topic**: `/ebimu_data` (`sensor_msgs/Imu`)

### 출력 포맷 (2026-08-22 실측)
현재 이 센서는 **자세 3개 필드만** 내보낸다.
```
*ROLL,PITCH,YAW        예: *-5.82,-2.39,135.80
```
9개 필드(`*ROLL,PITCH,YAW,ACCX,ACCY,ACCZ,GYROX,GYROY,GYROZ`) 설정도 가능하지만
지금은 그렇게 설정돼 있지 않다.

> ⚠️ **가속도·자이로 값이 없다.** 따라서 EKF 에서 `v_yaw` 와 선가속도를 쓸 수 없고,
> 절대 yaw 만 사용한다. `ebimu_publisher.py` 는 두 포맷을 모두 처리하며,
> 3필드일 때는 해당 항목의 `covariance[0]` 을 `-1`(미제공)로 표시한다.
> [[Debugging_Experience#11]]

## 제어 PC (Robot PC)
- **OS**: Ubuntu 20.04 LTS (2026-08-22 기준 실제 사용 환경)
- **ROS Version**: ROS 2 Foxy
- **비고**: 코드는 Foxy / Humble / Jazzy 모두에서 빌드된다. 자세한 호환표는 README 참고.
